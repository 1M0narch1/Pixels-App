<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PexelsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/background_app</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.PexelsApp" parent="Base.Theme.PexelsApp" >
        <item name="android:statusBarColor">@color/background_app</item>
    </style>

    <style name="Theme.PexelsSplash" parent="Theme.PexelsApp" >
        <item name="android:statusBarColor">@color/background_icon</item>
    </style>

    <style name="TextInputStyle" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="boxCornerRadiusBottomEnd">32dp</item>
        <item name="boxCornerRadiusBottomStart">32dp</item>
        <item name="boxCornerRadiusTopEnd">32dp</item>
        <item name="boxCornerRadiusTopStart">32dp</item>
        <item name="boxStrokeWidth">5dp</item>
    </style>

</resources>